name: CI Gates

on:
  pull_request:
  push:
    branches-ignore: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.11"
  UV_CACHE_DIR: /tmp/.uv-cache

jobs:
  bump-scope-guard:
    name: Bump Scope Guard
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      scope: ${{ steps.classify.outputs.scope }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Install anvil
        run: |
          pip install .
      - name: Classify change scope
        id: classify
        run: |
          echo "BUMP_SCOPE=full" > scope_output.txt
          cat scope_output.txt
          SCOPE=$(grep "^BUMP_SCOPE=" scope_output.txt | cut -d= -f2)
          echo "scope=${SCOPE}" >> "$GITHUB_OUTPUT"
      - name: Upload scope artifact
        uses: actions/upload-artifact@v4
        with:
          name: bump-scope-output
          path: scope_output.txt

  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: bump-scope-guard
    if: needs.bump-scope-guard.outputs.scope != 'version-only'
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@8d55fbecc275b1c35dbe060458839f8d30439ccf
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Setup environment
        run: make setup
      - name: Run lint
        run: make lint

  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: bump-scope-guard
    if: needs.bump-scope-guard.outputs.scope != 'version-only'
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@8d55fbecc275b1c35dbe060458839f8d30439ccf
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Setup environment
        run: make setup
      - name: Run tests with coverage
        run: make test

  gate-status:
    name: Gate Status
    runs-on: ubuntu-latest
    timeout-minutes: 3
    needs: [bump-scope-guard, lint, test]
    if: always()
    steps:
      - name: Summarize gate results
        run: |
          echo "## Gate Results" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Gate | Status |" >> "$GITHUB_STEP_SUMMARY"
          echo "|------|--------|" >> "$GITHUB_STEP_SUMMARY"
          for gate in bump-scope-guard lint test; do
            STATUS="${{ needs[gate].result }}"
            echo "| $gate | $STATUS |" >> "$GITHUB_STEP_SUMMARY"
          done
          echo "" >> "$GITHUB_STEP_SUMMARY"