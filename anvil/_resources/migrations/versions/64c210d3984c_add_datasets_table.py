"""Rev 003: No-op — tables already created by migration 001.

This migration was originally autogenerated against a development database
that was at revision 002 but whose tables from migration 001 had been
stripped. Every table it attempted to create (``license_catalog``,
``datasets``, ``corpora``, ``corpus_files``, ``training_configs``,
``import_sources``, ``curation_operations``, ``samples``,
``audit_events``) is already created by migration 001 — the squashed
canonical schema.

When run on a fresh database, 001 creates all of these tables, then 003
fails because it tries to ``CREATE TABLE`` on existing tables.

Fix: both upgrade and downgrade are no-ops. The tables are owned by
migration 001's upgrade/downgrade cycle.
"""

from __future__ import annotations

revision: str = "64c210d3984c"
down_revision: str | None = "002"
branch_labels: str | None = None
depends_on: str | None = None


def upgrade() -> None:
    """No-op — all tables already created by migration 001."""
    # Every table that this migration once tried to create
    # (license_catalog, datasets, corpora, corpus_files,
    # training_configs, import_sources, curation_operations,
    # samples, audit_events) is already created by migration 001.
    # See module docstring for full rationale.
    pass


def downgrade() -> None:
    """No-op — tables are owned by migration 001's downgrade."""
    pass
