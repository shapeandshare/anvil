"""Cross-domain types shared across service domains.

Internal infrastructure sub-package containing types, errors, and
utilities referenced by two or more domain sub-packages. Not a
domain itself — the underscore prefix signals this is internal.
"""
