"""Demo data bootstrap on first run.

Domain sub-package for first-run initialization — populating the
database with seed corpora and datasets so the application is
immediately usable after installation.
"""
